//! This is ran in google.

const images = document.querySelectorAll('.FFVAD')

images.forEach(img => imageUrlArray(img.src)); 

images.forEach(img => imageUrlArray.push(img.src));

imageUrlArray

copy(imageUrlArray)

//! This is just so I have quick access to this via my phone/computer/github account

2 + 2;
// Returns 4

(2 + 2);
// Returns 4

2 - 3;
// Returns -1

10 / 2;
// returns  5

2 * 10;
//returns 20

2 ** 10;
//returns 1024 (exponents) aka 2 to the power of 10

5 % 2;
// Returns 1 means its odd

10 % 2;
// returns 0

22222 % 2;
// returns 0 (means its even)

6 % 2;
// Returns 0 (means its even).

//? ++/-- has to be tied to a variable, will NOT work if not.

num++;
//Called Increment (++), changes after you call it, and then call it again.

++num;
// changes it when you return it.

num--;
//does the reverse of ++, which decreases but has to be called again (num)

--num;
// Returns all at once, minus.

//? Flip Value
var  someNum = 10;

var someOtherNum= -someNum;

someOtherNum;
// ! This is how its built to flip

//? How to change a string to a number
var strNum = '100';

var convertedNum = + strNum;
convertedNum;
//Returns 100, aka a number and not a string.

